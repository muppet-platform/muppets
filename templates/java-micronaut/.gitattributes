# Ensure binary files are handled correctly by Git
*.jar binary
*.class binary
*.zip binary
*.tar binary
*.gz binary
*.exe binary
*.dll binary
*.so binary
*.dylib binary

# Ensure shell scripts have Unix line endings
*.sh text eol=lf

# Ensure Gradle wrapper script has Unix line endings
gradlew text eol=lf

# Ensure text files have appropriate line endings
*.md text
*.txt text
*.yml text
*.yaml text
*.json text
*.xml text
*.properties text
*.gradle text
*.java text
*.kt text
*.groovy text

# Docker files
Dockerfile text
*.dockerfile text

# Configuration files
.env* text
.gitignore text
.gitattributes text