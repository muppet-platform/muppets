<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender with JSON formatting for structured logging -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp/>
                <logLevel/>
                <loggerName/>
                <message/>
                <mdc/>
                <arguments/>
                <stackTrace/>
            </providers>
        </encoder>
    </appender>

    <!-- Root logger - always use STDOUT for now -->
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>

    <!-- Application-specific loggers -->
    <logger name="com.muppetplatform.{{java_package_name}}" level="DEBUG"/>
    
    <!-- Micronaut framework loggers -->
    <logger name="io.micronaut" level="INFO"/>
    <logger name="io.micronaut.http.server" level="DEBUG"/>
    
    <!-- Reduce noise from common libraries -->
    <logger name="software.amazon.awssdk" level="WARN"/>
    <logger name="com.amazonaws" level="WARN"/>
    <logger name="org.apache.http" level="WARN"/>
</configuration>